services:
  - type: web
    name: dyad
    env: node
    plan: free
    buildCommand: |
      if [ -f tsconfig.json ]; then npm run build || true; fi
    startCommand: npm run start
    envVars:
      - key: NODE_ENV
        value: production
